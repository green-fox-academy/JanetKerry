'use strict';

// Create a function that takes a number
// divides ten with it,
// and prints the result.
// It should print 'fail' if the parameter is 0

let myFunction = 50; {
    if (myFunction / 10 == 0)
        console.log(myFunction) + ('fail');
}
