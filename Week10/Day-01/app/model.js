'use strict';

const getPlaylists = () => {
  // TODO
};

const getPlaylistTracks = playlistId => {
  // TODO
};

const postPlaylistTracks = (playlistId, track) => {
  // TODO
};

const deletePlaylistTracks = (playlistId, trackId) => {
  // TODO
};

module.exports = {
  getPlaylists,
  getPlaylistTracks,
  postPlaylistTracks,
  deletePlaylistTracks
};
